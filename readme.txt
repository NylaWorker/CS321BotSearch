AI, HW 2

To start a set of 100 simulations, simply run in terminal:
	python3 HeatMiser.py

The 'HeatMiserHeuristic.txt' file has to be in the same directory as HeatMiser.py. The program relies on importing Math, Random, Deque, Heap, and CSV, and no external libraries.